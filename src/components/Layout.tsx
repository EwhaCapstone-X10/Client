import React from 'react';
import {View} from 'react-native';
import Header from './Header';

const Layout = () => {
  return (
    <View>
      <Header />
      <View>main</View>
    </View>
  );
};

export default Layout;
